# ملف البرمجة الكامل والتعليمات التقنية: AvocatManager Pro (Maroc)

هذا الملف يحتوي على الدليل الكامل لإعداد وتشغيل برنامج إدارة مكتب المحاماة.

## 1. متطلبات النظام
- **Node.js**: الإصدار 16 أو أحدث.
- **MySQL**: الإصدار 8.0 أو أحدث.
- **متصفح حديث**: يدعم تقنيات React و CSS الحديثة.

## 2. إعداد قاعدة البيانات (MySQL)
1. قم بإنشاء قاعدة بيانات جديدة باسم `avocat_db`.
2. قم باستيراد الملف `schema.sql` المرفق لتوليد الجداول والعلاقات.
3. تأكد من إعداد المستخدم (غالباً `root` بدون كلمة سر في بيئة التطوير).

## 3. إعداد الجانب الخلفي (Backend - Node.js)
```bash
# إنشاء مجلد المشروع
mkdir lawyer-backend && cd lawyer-backend

# تهيئة المشروع
npm init -y

# تثبيت المكونات الضرورية
npm install express mysql2 cors body-parser dotenv

# تشغيل السيرفر (بعد نسخ كود server.js)
node server.js
```

## 4. إعداد الواجهة الأمامية (Frontend - React & Vite)
تم استخدام Vite لسرعته الفائقة في التطوير.
```bash
# إنشاء مشروع React مع TypeScript
npm create vite@latest lawyer-frontend -- --template react-ts

# الانتقال للمجلد
cd lawyer-frontend

# تثبيت المكونات
npm install

# تثبيت أيقونات Font Awesome (عبر CDN المذكور في index.html)
# أو يدوياً: npm install @fortawesome/fontawesome-free

# تشغيل الواجهة
npm run dev
```

## 5. هيكلية الواجهة (UI/UX)
- **الألوان**: تم اختيار ألوان هادئة (Soft Colors) مثل Indigo للهوية، Emerald للإضافة، و Rose للمالية.
- **اللغة**: التطبيق يدعم اللغة العربية بالكامل (RTL) مع خط Tajawal.
- **التصميم**: جميع النماذج (Forms) داخل `w3-card-4` لتحقيق العمق البصري المطلوب.
- **الرموز**: تم استخدام Font Awesome 6.4 قبل كل ملصق (Label) كما هو مطلوب.

## 6. الربط مع نظام محاكم (Mahakim.ma)
بما أن البوابة الرسمية لا توفر API عامة، يعتمد النظام على:
- **الرقم الفريد**: تخزين رقم الملف بصيغة محاكم الرسمية.
- **الروابط الذكية**: توليد روابط مباشرة لصفحة تتبع القضايا في بوابة محاكم لتوفير وقت البحث.

---
**إعداد وتطوير:** Senior Frontend Engineer
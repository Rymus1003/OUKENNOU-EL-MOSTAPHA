# دليل تثبيت وتشغيل "المحامي برو" (AvocatManager Pro)

## أولاً: إعداد قاعدة البيانات
1. افتح برنامج **XAMPP** أو **WampServer**.
2. ادخل إلى **phpMyAdmin**.
3. قم بإنشاء قاعدة بيانات جديدة باسم `avocat_db`.
4. اختر تبويب "SQL" وقم بلصق محتويات الملف `schema.sql` ثم اضغط "Execute".

## ثانياً: إعداد الخادم (Backend)
1. افتح منفذ الأوامر (Terminal) في مجلد المشروع.
2. تأكد من وجود ملف `server.js`.
3. قم بتثبيت المكتبات المطلوبة:
   ```bash
   npm install express mysql2 cors body-parser dotenv
   ```
4. قم بتشغيل السيرفر:
   ```bash
   node server.js
   ```
   *ستظهر رسالة: SUCCESS: Connected to MySQL (avocat_db)*

## ثالثاً: إعداد الواجهة (Frontend)
1. افتح نافذة أوامر جديدة.
2. قم بتثبيت مكتبات React (إذا كنت تستخدم Vite):
   ```bash
   npm install
   ```
3. ابدأ تشغيل التطبيق:
   ```bash
   npm run dev
   ```

## ملاحظات هامة:
- **نظام محاكم**: البرنامج يوفر روابط ذكية تنقلك مباشرة إلى بوابة محاكم مع إمكانية التحقق من أرقام الملفات.
- **التصميم**: تم استخدام `w3-card-4` لجميع النماذج مع الحفاظ على تناسق الألوان (Indigo للنظام، Rose للمالية، Emerald للإضافة).
- **اللغة**: التطبيق يدعم الكتابة بالعربية (RTL) بشكل أصيل.
- **وضع الاحتياط (Offline)**: في حالة عدم تشغيل قاعدة البيانات، سيقوم البرنامج تلقائياً بالتحويل إلى `LocalStorage` للسماح لك بتجربة الواجهة.